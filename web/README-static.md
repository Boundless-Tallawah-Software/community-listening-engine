# Web Front‑End

The `web/` directory hosts the static front‑end assets served by the application.  The files under this directory are plain HTML/JS/CSS and are *not* Python code.

These files are served as static files from the Cloudflare worker or via the embedded front‑end server.  No Python dependencies are required to run the front‑end.
